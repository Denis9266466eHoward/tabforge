const {
  createStore,
  setHotkey,
  removeHotkey,
  getHotkey,
  getSnapshotForHotkey,
  listHotkeys,
  clearHotkeysForSnapshot,
} = require('./hotkey');

module.exports = {
  createStore,
  setHotkey,
  removeHotkey,
  getHotkey,
  getSnapshotForHotkey,
  listHotkeys,
  clearHotkeysForSnapshot,
};
